spring.application.name=recruitment

# --- Databas (Supabase) ---
# Adressen hämtad från din bild
spring.datasource.url=jdbc:postgresql://db.leydptslkucecqaozmsb.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
# VIKTIGT: Byt ut detta mot lösenordet du kopierade i första steget!
spring.datasource.password=KungliglaTekniskaHogskolan123

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

# --- Flyway (Magin som löser Task 28) ---
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
# Denna rad gör att Flyway accepterar att Supabase inte är helt tom
spring.flyway.baseline-on-migrate=true

# --- Övrigt ---
spring.thymeleaf.prefix=classpath:/templates/
logging.level.root=INFO